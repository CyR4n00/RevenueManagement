## 2026-06-21 - [Date Input Accessibility]
 **Learning:** [Using both `<label className="sr-only">` and `aria-label` on the same input is redundant for screen readers.]
 **Action:** [Avoid combining sr-only labels with aria-labels; pick one method to provide accessible names to inputs to avoid verbosity.]
